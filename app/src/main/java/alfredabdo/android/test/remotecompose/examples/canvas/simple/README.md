# Canvas Simple Example

A simple example that shows how to use **RemoteCanvas**.

![Canvas Simple Example](images/example.gif)<br>Canvas Simple Example

_Some notes_:
- The function _inset_ is not yet available, so the workaround now was to rely on _translate_ and _clipRect_.